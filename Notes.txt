NOTES:

1) Before inserting to database or before sending to backend, make sure all data must be set to lowercase

2) Relations
   a) Study relations

3) Settings
   a) Fix the settings pop up, it is the atom.
   b) Make a logic which if user clicks outside of the settings then setting nav should close.

4) Navigator: To check if online or offline

5) Improved the stacks and socials
   a) improved the styles

6) Make all the data or values to be a lowercase before inserting to database

7) In every protected api request, attached and validate the token in the headers