NOTES:

1) Login:
   a) if login status is ok, return also the ID in the response
   b) use the ID and store it to localstorage. ( use cookies or tokenize )
   3) current ID stored in the localstorage will be use in the update profile page

2) Relations
   a) Study relations

3) Settings
   a) Fix the settings pop up, it is the atom.
   b) Make a logic which if user clicks outside of the settings then setting nav should close.